server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Handle React Router - this is where try_files belongs
    location / {
        try_files $uri $uri/ /index.html;
    }
}